# SCTP-javascript-DOM
 8103-dom

To see your website, in the terminal, type
`python -m http.server`
this should show a pop which says open in browser
otherwise switch to 'ports' view and click on the https link nex to 8000
This is the most accurate to test the code on a browser
